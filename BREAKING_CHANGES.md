# Breaking Changes

This log records every backward-incompatible change to Prompture's
**stable** APIs. Experimental surfaces may change without an entry here
(see [API Stability](docs/source/stability.rst) for tier definitions).

Changes are listed newest-first.

---

## Unreleased

_No breaking changes yet._

<!-- Template for future entries:

## vX.Y.Z (YYYY-MM-DD)

### Removed
- `old_function()` — Use `new_function()` instead. Deprecated since vA.B.C.

### Changed
- `some_function()` parameter `old_param` renamed to `new_param`.

### Migration
- Replace `old_function(x)` with `new_function(x)`.

-->
